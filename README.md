Collection of CTI tools
- domain_scanner.py: DNS resolution to find domains hosted on the same IP
- bulk_domain_suspension_check.py: A tool to scan all cases from an API, extract domains, run WHOIS queries,
and detect suspended statuses (clientHold / serverHold).